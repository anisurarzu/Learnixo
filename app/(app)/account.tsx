import { PlaceholderScreen } from '@/components/navigation';

export default function AccountScreen() {
  return (
    <PlaceholderScreen
      title="Account"
      subtitle="Email, password, connected providers, and danger zone."
      icon="shield-checkmark-outline"
    />
  );
}
