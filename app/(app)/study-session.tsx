import { PlaceholderScreen } from '@/components/navigation';

export default function StudySessionScreen() {
  return (
    <PlaceholderScreen
      title="Study session"
      subtitle="Timed focus session UI — connect planner logic next."
      icon="timer-outline"
    />
  );
}
